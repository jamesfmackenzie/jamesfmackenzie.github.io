source "https://rubygems.org"
gem "jekyll"

# Add any necessary plugins for GitHub Pages or your local build
gem "github-pages", group: :jekyll_plugins

# Gems required only for local validation (GitHub Pages ignores this group)
group :development do
  gem "html-proofer", "~> 4.0"
end